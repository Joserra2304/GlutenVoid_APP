name: glutenvoid_app
description: "Desarrollo de una aplicación movil para usuarios con trastornos alimenticios provocados por el gluten"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.2.11
  http: ^0.13.4
  google_ml_kit: ^0.16.3
  image_picker: ^0.8.4+4
  google_maps_flutter: ^2.6.1
  geocoding: ^3.0.0
  provider: ^6.1.2

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.9.2"

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/gluten_void_icon.png"
  adaptive_icon_background: "#FFFFFF"

  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/images/gv_animated.gif
    - assets/images/gv_animated.mp4
    - assets/images/gluten_void_logo.png

  uses-material-design: true

